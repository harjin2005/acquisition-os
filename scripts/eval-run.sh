#!/usr/bin/env bash
# scripts/eval-run.sh — placeholder. Sprint 1 does not implement agent evals.
# E10 lands the Langfuse runner + threshold enforcement.
set -euo pipefail
agent="${1:-}"
echo "!! Sprint 1: eval runner is a placeholder. Agent evals land in E10 (Sprints 8-13)."
echo "   Agent requested: $agent"
exit 0
